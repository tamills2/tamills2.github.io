# Note copy button content-position fix

Replace:

- `public/index.html`
- `public/css/styles.css`

The existing `public/js/main.js` from the copy-button patch remains unchanged.

Then rebuild:

```bash
python3 scripts/build_site.py
```
