Replace:

public/tools/timezones/tool.css

This patch keeps the timezone grid at 5 columns on all screen sizes and
scales the clock cards using responsive font sizes instead of changing the
number of columns.
