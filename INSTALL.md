# Hide Tool Template update

Replace the included files at the same paths in your repository.

This update:
- removes the hard-coded Tool Template link from the Regex Generator menu;
- keeps the template folder available for copying;
- explicitly marks the template as hidden from tool discovery;
- removes the stale Tool Template phrase from generated search data.
